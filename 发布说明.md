# Airwaysn 语音客户端

两个 Windows 程序，解压后直接运行 exe，不需要装 Python 或任何依赖。

> 自己从源码构建的话注意：能跑的是 `dist/` 里那份。`build/` 是 PyInstaller 的
> 工作目录，里面有个同名 exe 但缺少 `python312.dll` 等依赖，点了会报
> 「Failed to load Python DLL」。

| 程序 | 用途 |
|---|---|
| `airwaysn-controller` | 管制语音客户端，可同时守听/发话多个频率 |
| `airwaysn-atis` | 情报通播客户端，按 METAR 自动生成并循环播出 |

账号就是网站账号（ASN 号 + 密码），未评级（rating < 1）的账号无法使用语音。

---

## 管制语音客户端

1. 双击 `airwaysn-controller.exe`，填用户名和密码，点连接
2. 在上方输入频率（如 `118.000`）和呼号（如 `ZSPD_TWR`），点「添加频率」
3. 每个频率一行，三个开关：

   | 开关 | 含义 |
   |---|---|
   | **RX** | 接收这个频率 |
   | **TX** | 按住 PTT 时对这个频率发话 |
   | **XC** | 与其它开了 XC 的频率交叉耦合（合并席位时用） |

   三者有联动：开 TX 会自动开 RX，开 XC 会自动开 RX+TX，关掉 RX 则三个一起关。

4. 按住 PTT 键说话（默认 `V`，可在设置里改），对**所有开了 TX 的频率**同时发话
5. 点某一行可以把它设为**主频率**（标 `▸`）

每行右边有独立音量和静音，左下角显示这个频率最后是谁在讲话。

**关于多频率接收**：同时收多个频率依赖服务器支持频道监听（Mumble 1.4 以上）。
若只听得到主频率，说明服务器版本不够——界面状态栏会提示。

## 情报通播客户端

1. 双击 `airwaysn-atis.exe`
2. 「新建」一个席位：机场 ICAO、频率、类型（综合/离场/进场）
   - 类型决定呼号：`ZSPD_ATIS` / `ZSPD_D_ATIS` / `ZSPD_A_ATIS`
   - 情报字母范围可以分开，避免离场和进场的通播被搞混
3. 「编辑预设」写模板，用方括号变量引用天气：

   ```
   [FACILITY] ATIS [ATIS_LETTER] [OBS_TIME]. [WX]. [ARPT_COND] [NOTAMS] [CLOSING]
   ```

   常用变量：`[WIND]` `[VIS]` `[CLOUDS]` `[TEMP]` `[DEW]` `[PRESSURE]` `[RVR]`
   `[PRESENT_WX]` `[TREND]` `[TL]` `[ARPT_COND]` `[NOTAMS]` `[ATIS_LETTER]`

   变量后加 `:VOX`（如 `[WX:VOX]`）表示在文字通播里也用语音说法。

4. 填好账号密码，点「开始播出」

**已经在用 vATIS 的话**，左下角「导入 vATIS 配置…」可以直接读它的 profile
JSON（vATIS 里导出，或者从 `%APPDATA%\org.vatsim.vatis\Profiles\` 里拿），
席位、频率、情报字母范围、预设模板、缩略语都会一起导进来。vATIS 里本客户端
没有对应功能的设置（细粒度读法选项、IDS 推送、短语库、语音录制）会跳过，
导入完成时会列出来。

天气每 5 分钟自动刷新，METAR 一变就自动推进一格情报字母并换稿——换稿在下一轮
生效，不会打断正在播的这遍。频率上有人讲话时会暂停让路。

**装机后建议先跑一次自检**，确认这台机器的语音合成可用：

```
airwaysn-atis.exe --selftest
```

退出码 0 表示正常。失败一般是系统缺少语音包：Windows 设置 → 时间和语言 →
语音 → 添加语音。中文通播需要中文语音包。

---

## 出问题时看日志

两个程序都会在**自己所在目录**写日志：

```
airwaysn-controller.log
airwaysn-atis.log
```

设置里有「打开日志」按钮，直接跳到文件所在目录。

报问题时请连日志一起发。如果是连不上、听不到、通播没声音这类问题，先在设置里
勾上「记录调试信息」再重启复现一次——那一档会把协议细节也记下来（FSD 收发的
每个包、进出了哪些频道、发话目标是什么），排查起来快得多。命令行加 `--debug`
效果相同。

日志滚动保留 4 份，每份最大 2 MB，不会无限长大。密码不会写进日志。

## 配置文件

程序在**当前目录**下读写配置，所以建议放在固定文件夹里运行：

| 文件 | 内容 |
|---|---|
| `radio_settings.json` | 管制端：PTT 键、音量、音频设备、电台栈 |
| `atis_profile.json` | 通播端：所有席位和预设 |
| `atis_settings.json` | 通播端：账号、气象源地址 |

## 已知限制

- 多频率同时接收要 Mumble 1.4 以上的服务器
- 通播的语音质量取决于系统自带的 SAPI 语音
- 音频设备若不支持 48kHz，会退到较低采样率，音调会有偏差
